import <%= props.name %> from "./<%= props.name.toLowerCase() %>";
import { validate } from "uuid";

describe("<%= props.name %> Unit Tests", () => {
  test("constructor of <%= props.name.toLowerCase() %>", () => {
    const <%= props.name.toLowerCase() %> = new <%= props.name %>({});

    expect(<%= props.name.toLowerCase() %>.props).not.toBeNull();
    expect(validate(<%= props.name.toLowerCase() %>.id)).toBeTruthy();
  });
});
